package com.example.online_bank.security.jwt.service;import org.springframework.stereotype.Component;import javax.crypto.SecretKey;import java.util.Base64;/** * Encode делает строку из байтов * (то есть byte[] → String, через Base64.getEncoder) */@Componentpublic class SecretKeyEncoder {    public String encode(SecretKey secretKey) {        byte[] secretKeyBytes = secretKey.getEncoded(); // получаем байты секретного ключа        String encodedToStringSecretKey = Base64                .getEncoder() //получаем вложенный Encoder из класса Base64  Base64.Encoder encoder = Base64.getEncoder();                .encodeToString(secretKeyBytes);        return encodedToStringSecretKey;    }}